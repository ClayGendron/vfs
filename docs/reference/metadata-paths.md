<!--
DIÁTAXIS TYPE: Reference (information-oriented)
THE RULE: This is the on-disk/wire CONTRACT. Model it on the SQLite file
format spec: every path segment in a table, every reserved constant explicit,
and for each fixed invariant state WHY it is fixed.
SOURCE TO MIGRATE FROM: docs/index.md "Metadata Namespace"
-->

# Metadata path format

The `/.vfs/<endpoint>/__meta__/...` namespace.
