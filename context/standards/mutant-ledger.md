# Mutant ledger — proven bugs the suite must keep killing

**Status:** live — founded 2026-08-25 (ADR 050); appended to by every
review campaign that lands pins.
**Purpose:** Each row is a one-line bug that once survived the whole
suite and was killed by a landed pin. Replaying a row re-proves the
pin still has teeth. Replays run **during review campaigns** — the
`test_review` skill owns the procedure — not as standing CI.

## Replay protocol

**Never mutate the live repo** — review agents run concurrently
against it, and a briefly-mutated `src/` poisons every neighbor's
reads and test runs. All replays happen in an isolated tree under the
session scratchpad: `git worktree add <scratchpad>/mutant-tree
<tip-sha>`, primed once with `uv sync`, removed when done (the
`test_review` skill carries the full protocol).

For each row in scope (target file touched by the review, plus any
row when time allows — the full ledger replays in ~2 minutes):

1. Apply the mutation **in the worktree**. Rows record **intent plus
   the best-known anchor**; if the anchor text has moved, re-derive
   the mutation from the intent before declaring anything.
2. Run the row's scope selection from the worktree. **Killed** = ≥1
   failure.
3. Restore the worktree file from its committed state
   (`git -C <worktree> checkout -- <file>`) and confirm the worktree
   is clean before the next row.

Statuses, all reported: **killed** (healthy), **survived** (a pin
regressed — a critical review finding), **stale** (the mutation's
concept no longer exists in the code — propose retiring the row, with
the reasoning; never silently skip). The *recorded killers* column is
advisory diagnosis only — which test kills a mutant drifts even when
protection holds (see M5, the founding example) — the assertion is
always "≥1 failure in scope", never a named test.

## Rows

| id | mutation (intent → anchor) | target | scope | recorded killers (advisory) | provenance |
|---|---|---|---|---|---|
| R1 | `_channel_facts` emits a partial OR instead of voiding the channel (the void `return None` arm becomes `continue`) | `src/vfs/storage/backends/database/grep.py` | `tests/storage/` | mixed-channel storage row; `_channel_facts` void unit row; contract battery mixed-channel row (memory leg) | spec 109 |
| R2 | `_arm_ids` intersects only the rarest term: `ranked[:_INTERSECT_TERMS]` → `ranked[:1]` | `src/vfs/storage/backends/database/pathterms.py` | `tests/storage/database/` | term-overlap decoy row; wide-arm slice row; `test_multi_term_arms_intersect` | spec 109 |
| R3 | `_CANDIDATE_COST_US` 75.0 → 7.5 | `src/vfs/storage/backends/database/grep.py` | `tests/storage/database/` | `_ladder_defers` boundary rows (4 failures re-verified 2026-08-25) | spec 109 |
| R4 | `_CANDIDATE_COST_US` 75.0 → 750.0 | same | same | ladder boundary rows (3) | spec 109 |
| R5 | `_GROUP_SETUP_US` 500.0 → 5000.0 | same | same | ladder boundary rows (4) | spec 109 |
| R6 | `_GROUP_SETUP_US` 500.0 → 50.0 | same | same | ladder boundary rows (3) | spec 109 |
| R7 | `_POSTING_COST_US_PER_BYTE` 0.055 → 0.55 | same | same | defer/ladder spy row (1) | spec 109 |
| R8 | `_POSTING_COST_US_PER_BYTE` 0.055 → 0.0055 | same | same | defer/ladder spy row (1) | spec 109 |
| M1 | delete flushes only its last root posting delta: the accumulated `segment_moves` batch → `segment_moves[-1:]` at the `move_postings` flush | `src/vfs/storage/backends/database/topology.py` | `tests/storage/` | batch delete row; batch restore row | spec 113 |
| M2 | `_execute_move` mirrors only the batch's last pair (identity at width 1) | same | same | batch move row; batch restore row | spec 113 |
| M3 | grep's hoisted projection or row-mask is widened by the ridden row-gate columns, at the `projected`/`row_mask` hoist above the assembly loop — **both directions are the mutation**: mask alone (a wire-mask leak) and projection alone (a valued field outside the mask) | `src/vfs/storage/backends/database/grep.py` | `tests/storage/` | mask direction: grep exact-mask row (`columns={"content"}`); projection direction: the same row's `size_bytes is None` value assert + the conformance mask law's narrow-columns grep rows (3 failures, proven 2026-08-25) | spec 113; projection direction pinned + anchor tightened by spec 124 |
| M4 | glob observes the queried mask instead of the requested one | `src/vfs/storage/backends/database/reads.py` | `tests/storage/database/` | glob exact-mask row (`columns={"path"}`) | spec 113 |
| M5 | the overlay-emptiness verdict is presumed verified: `skip_verified = False` → `True` | `src/vfs/storage/backends/database/grep.py` | `tests/storage/database/test_grep.py` | recorded: the epoch-spy rescued-verdict row — **drifted 2026-08-25**: that row now passes under the mutant; killed instead by two `TestEpochLadder` rows and the scan-path statement-count pin (3 failures). The row that made killers advisory-only. | spec 113 |
| C1 | ladder-defer comparator tie at the exact crossover (`allow_size=80`) — flip the comparator's strictness | `src/vfs/storage/backends/database/grep.py` | `tests/storage/database/` | **none — designed-inert**: the tie is observationally inert, so this row is *expected to survive*. Carried for visibility; it becomes a real row only if a comparator-strictness pin ever lands. Never report it as a regression. | remediation decision pass 2026-08-18 |
| P1 | fingerprint-skip ignores the chunk generation (drop `row.chunk_generation == generation` from the skip condition) | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `TestChunkProvenance::test_generation_change_redirties_and_resplits` (1 failure, proven 2026-08-25) | spec 117 slice C |
| P2 | fingerprint-skip ignores the body hash (drop `row.content_hash == row.chunk_source_hash`; skip fires on any current-generation row) | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `TestChunkProvenance::test_changed_body_still_resplits` (1 failure, proven 2026-08-25) | spec 117 slice C |
| P3 | the verify budget cut moves to submit time (`deadline - monotonic()` hoisted out of the worker fn in both arms) — queue wait silently extends the wall | `src/vfs/storage/backends/database/offload.py` | `tests/storage/database/test_offload.py` | the two queue-wait budget rows (2 failures, proven 2026-08-25) | spec 118 slice A |
| P4 | the one-in-flight guard is deleted (`if self._in_flight: raise` removed), letting a second batch race the first onto the pool | `src/vfs/storage/backends/database/offload.py` | `tests/storage/database/test_offload.py` | `TestVerifyOffload::test_overlapping_calls_are_refused` (1 failure, proven 2026-08-25) | spec 118 slice A |
| P5 | verify runs inline: `_run`'s `run_in_executor` call becomes `return guarded()`, holding the loop for the whole batch | `src/vfs/storage/backends/database/offload.py` | `tests/storage/database/test_offload.py` | the tick-gap responsiveness row; every `TestAbandonment` row (8 failures, proven 2026-08-25) | spec 118 slice C |
| P6 | the generation re-dirty runs unconditionally (probe deleted, the UPDATE issued on every pass) — RR engines scan-lock the whole entry table in the steady state | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `test_flag_flips_are_set_based_not_per_row` + `test_steady_state_reindex_issues_no_entry_update` (2 failures, proven 2026-08-25); the MySQL lock-scope race row on the live leg | spec 121 |
| P7 | the probe's verdict is inverted (`is not None` → `is None`) — stale generations never re-dirty | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `TestChunkProvenance::test_generation_change_redirties_and_resplits` (1 failure, proven 2026-08-25) | spec 121 |
| P8 | close shuts the offload pool but keeps the dead slot (`self._offload_executor = None` removed; the slot was named `_verify_executor` before spec 120's rename) — every later offloaded call limps through the fallback forever | `src/vfs/storage/backends/database/engine.py` | `tests/storage/database/test_offload.py` | `test_close_shuts_the_pool_down_without_waiting`'s re-mint asserts (1 failure, proven 2026-08-25) | spec 122; anchor renamed by spec 120 |
| P9 | the close-window inline fallback re-raises instead of serving (`return guarded()` → `raise`) — a grep racing close escapes a raw RuntimeError | `src/vfs/storage/backends/database/offload.py` | `tests/storage/database/test_offload.py` | `test_a_grep_racing_close_is_served_whole` + `test_a_reindex_racing_close_is_served_whole` (2 failures, re-proven 2026-08-25) | spec 122; reindex row added by spec 129 |
| P10 | the ready latch survives close (`self._ready = False` removed) — the first op after close serves a catalog miss off the stale latch | `src/vfs/storage/backends/database/engine.py` | `tests/storage/test_conformance.py` | `TestMemoryConformance::test_pattern_search_serves_after_close` (1 failure, proven 2026-08-25) | spec 122 |
| P11 | the kernel-metadata reads lose their isinstance guards (revert to `.get(...) or {}` / `.lower()` chains) — a valid .ipynb with non-string `kernelspec.language` raises raw AttributeError and wedges every reindex | `src/vfs/models/chunking.py` | `tests/models/` + `tests/storage/database/test_indexing.py` | metadata degradation battery, `test_split_batch_matches_per_file_splits_across_routes`, `test_a_malformed_notebook_never_wedges_the_reindex` (3 failures, proven 2026-08-25) | spec 123 |
| P12 | the whole-text slice-end zero-width discard loses a direction — over-serve: the guard deleted whole (phantom boundary matches, cap displacement); over-discard: the `and stop < len(text)` EOF qualifier deleted (genuine `$` matches at end of unterminated bodies silently lost, budgeted and unbudgeted). Anchor: the guard lives once, in the `_whole_matches` driver both whole-text consumers share (unified by spec 125; both directions replayed and killed there, 4 failures each) | `src/vfs/pattern_matching/grep.py` | `tests/pattern_matching/test_matcher_parity.py` | over-serve: fabrication/displacement/boundary rows (4, proven at spec 112); over-discard: `test_a_zero_width_match_at_eof_is_served` + the `$` CASE riding every sweep (4 failures, proven 2026-08-25) | specs 112 + 124; anchor re-derived by spec 125 |
| P13 | the candidate-budget truncation record duplicates at the storage seam: **both** dedupe guards deleted — the index-side cut's and the scan-overflow's `if "candidate budget" not in truncations` — so each tier emits and only the facade's merge hides the twin. The scan-side guard alone kills too. The index-side guard **alone is designed-inert**: `truncations` is provably empty at that cut (probe-assert never fired across the suite plus a live-Postgres leg), so that direction is *expected to survive* — never report it as a regression; the guard stays as defensive symmetry (spec 125's ruling, re-affirmed 2026-08-25) | `src/vfs/storage/backends/database/grep.py` | `tests/storage/database/test_grep.py` | `TestBudgets::test_the_candidate_budget_record_is_emitted_at_most_once` (1 failure for both-deleted and for scan-side-alone; index-side-alone survives by design — all three replayed 2026-08-25) | spec 125; re-worded by spec 129 (F8) |
| P14 | a reindex CPU hop reverts to inline, holding the event loop for the stage's whole wall — three mutations, each executed: `chunk_dirty`'s assess-and-split call, `build_epoch`'s fold-and-feed calls, or the drain loop's `call_offloaded` replaced by the direct call | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `TestReindexLoopResponsiveness::test_the_loop_keeps_ticking_through_every_reindex_hop` (1 failure per direction, proven 2026-08-25) | spec 120 |
| P15 | the byte batcher loses its trailing batch (`byte_chunked`'s final flush-and-yield deleted) — the last batch's items silently vanish from every consumer (split targets emit no chunk rows, grep's last content batch never verifies) | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/` | `TestSplitBatchBudget` equality + spy rows, grep batcher rows (76 failures, re-proven at the shared anchor 2026-08-25) | spec 120; anchor re-derived by spec 128 (the copy it named died into `byte_chunked`) |
| P16 | the close gate returns: a `_closed` latch reintroduced around `close()`'s ready-drop and dispose — the second close strands every re-minted connection, and a cancelled close's retry reclaims nothing | `src/vfs/storage/backends/database/engine.py` | `tests/storage/database/test_engine.py` | `test_every_close_releases_what_revival_re_minted` + `test_a_cancelled_close_retry_finishes_the_job` (2 failures, proven 2026-08-25) | spec 126 |
| P17 | the offload shutdown reverts to `shutdown(wait=False, cancel_futures=True)` — a batch queued at close dies as raw CancelledError across the seam instead of being served | `src/vfs/storage/backends/database/engine.py` | `tests/storage/database/test_offload.py` | `test_queued_work_at_close_is_served_never_cancelled` (1 failure, proven 2026-08-25) | spec 126 |
| P18 | the notebook parse guard loses its recursion arm (`RecursionError` dropped from the except tuple) — deep-nested JSON raises raw and wedges every reindex until the file is deleted | `src/vfs/models/chunking.py` | `tests/models/` + `tests/storage/database/test_indexing.py` | deep-nest battery (both depths) + `test_a_deep_nested_notebook_never_wedges_the_reindex` (3 failures, proven 2026-08-25) | spec 127 |
| P19 | the encode floor falls — two directions, each executed: the `_scrub_unstorable` call deleted from `split_code_batch` (manufactured surrogates/NULs reach the encode and the chunk model raw); `normalize_content` reverts to strict `encode("utf-8")` (the recursive fallback and the gram stream raise on a surrogate) | `src/vfs/models/chunking.py` + `src/vfs/models/code_grams.py` | `tests/models/` + `tests/storage/database/test_indexing.py` | scrub direction: the `TestExceptionFloor` surrogate/NUL battery + `test_a_surrogate_escape_notebook_never_wedges_the_reindex` (8 failures); strict direction: `test_the_recursive_splitter_never_raises_on_a_surrogate` + `test_a_lone_surrogate_meters_instead_of_raising` (2 failures; both proven 2026-08-25) | spec 127 |
| P20 | a byte-budgeted stage reverts to whole-set — three directions, each executed: the split loop takes `[list(split_targets)]` whole; the extract batcher minted with a `1 << 60` budget (one whole-corpus feed); the posting drain called with a `1 << 60` cap (one giant insert) | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | split: `test_the_split_batcher_actually_slices`; extract: `test_extraction_feeds_flush_mid_stream_at_the_byte_budget`'s feed count; drain: `test_posting_drain_actually_slices_by_the_byte_cap` (1 failure per direction, proven 2026-08-25) | spec 128 |
| P21 | a declared batch budget is voided in place — either direction: `_SPLIT_BATCH_BYTES` or `_POSTING_BATCH_BYTES` set to `1 << 60` at the declaration | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_indexing.py` | `test_budgets_keep_their_declared_identities` (1 failure per direction, proven 2026-08-25 — no monkeypatch involved) | spec 128 |
| L1 | the epoch fingerprint ignores the lexical index: `index_options_hash` drops `options_fingerprint()` (a k1/b/tokenizer/block-size change no longer forces a rebuild) | `src/vfs/storage/backends/database/indexing.py` | `tests/storage/database/test_lexical.py` | `TestFingerprint` (3 failures, re-proven 2026-08-26 on the second landing) | spec 130 |
| L2 | the lexical scan loses its liveness gate: `entry.c.deleted_at.is_(None)` dropped from `_chunk_batches`' predicate — a trashed entry's chunks are indexed into the next epoch | `src/vfs/storage/backends/database/lexical.py` | `tests/storage/database/test_lexical.py` | `test_a_trashed_entry_leaves_the_next_epoch` (1 failure, re-proven 2026-08-26) | spec 130 |
| L3 | a reclaim forgets the lexical tables: `VFSTables.epoch_scoped()` returns only `(posting_list, gram_epochs)` | `src/vfs/models/rows.py` | `tests/storage/database/test_lexical.py` + `tests/models/test_rows.py` | the `TestReclaim` rows + the epoch-scoped table pin (5 failures, re-proven 2026-08-26) | spec 130 |
| L4 | document length counts distinct terms: `dl = len(tokens)` → `dl = len(counts)` in `PureLexicalBuilder.add_docs` — the pure engine's rows drift from the Rust engine's | `src/vfs/models/lexical.py` | `tests/models/test_lexical.py` + `tests/test_native.py` | the dl pin, the sealed-blocks row, the builder parity rows (6 failures, re-proven 2026-08-26) | spec 130 |
| L5 | idf loses Lucene's smoothing: `log(1 + (N - df + 0.5)/(df + 0.5))` → `log((N - df + 0.5)/(df + 0.5))` (negative for common terms; the pure summary rows drift from Rust) | `src/vfs/models/lexical.py` | `tests/models/test_lexical.py` + `tests/test_native.py` | `test_idf_is_never_negative`, the summary pins, the parity rows (8 failures, re-proven 2026-08-26) | spec 130 |
| L6 | the build's chunk scan reverts to a streaming cursor (`session.stream(..., yield_per=…)` in place of the keyset pages) — writes between batches hit an open cursor on drivers without MARS | `src/vfs/storage/backends/database/lexical.py` | `tests/storage/database/test_lexical.py` (+ the `…LexicalBuildBeyondAPage` engine rows) | `test_the_scan_reads_keyset_pages_never_an_open_cursor` (1 failure, re-proven 2026-08-26; the MSSQL leg row reproduces the original failure at 600 chunks) | spec 130 |
| L7 | a block's summary maximum falls below its true maximum: the Rust finisher's `fold(0.0, f64::max)` over the block's weights becomes a `min` — block selection leaves a competitive block unfetched | `crates/vfs-core/src/lexical.rs` | `tests/storage/database/test_lexical.py` + `tests/test_native.py` | `test_the_two_round_fetch_ranks_as_the_whole_fetch`, the summary pins in the build row, the summary-row parity rows (5 failures, proven 2026-08-26 — a wheel rebuild in the worktree) | spec 130 |
| L8 | the pure scorer's tie-break drifts from the Rust engine's (`np.lexsort((uniq, -scores))` → `(-uniq, -scores)`: ties fall to the higher chunk id) | `src/vfs/models/lexical.py` | `tests/test_native.py` + `tests/models/test_lexical.py` | `test_scores_identical` on the tie-rich spanning corpus, the scorer order pin (4 failures, proven 2026-08-26) | spec 130 |
| L9 | the fingerprint drops the wire format: `options_fingerprint()` loses `block=` and `codec=` — a block-size or codec change reads a stale index | `src/vfs/models/lexical.py` | `tests/storage/database/test_lexical.py` | `test_a_block_size_or_codec_change_forces_a_rebuild` (1 failure, proven 2026-08-26) | spec 130 |
| L10 | a term's summary omits its last block (`self.blocks()[:-1]` in `_TermList.summary`) — the tail block can never be selected for round two | `src/vfs/models/lexical.py` | `tests/test_native.py` + `tests/storage/database/test_lexical.py` under `VFS_PURE_PYTHON=1` | the summary-row parity rows, the build row, the two-round referee (10 failures, proven 2026-08-26) | spec 130 |
| L11 | block selection skips a tie at θ: `competing_blocks`' `>=` becomes `>` — a block whose lift lands exactly on the k-th score is left in the engine and the ranking changes | `src/vfs/models/lexical.py` | `tests/models/test_lexical.py` + `tests/storage/database/test_lexical.py` | `TestCompetingBlocks` and the two-round referee on the tie-rich spanning corpus (1 failure, proven 2026-08-26) | spec 130 |
| L12 | the reindex beat task reads any `conflict` as a taken lease (`lease_lost(beat)` → `any(error.kind == conflict …)`) — a beat stalled behind the build's own SQLite write lock past its retries stops the run mid-build (hit at full linux scale on the second landing) | `src/vfs/storage/backends/database/backend.py` | `tests/storage/database/test_indexing.py` | `test_a_transport_conflict_is_not_a_taken_lease` (1 failure, proven 2026-08-26) | spec 130 (defect in spec 120's lease) |
| B1 | a bulk insert's bind processors are dropped (`_bulk_values` hands the driver raw values) — a `DateTime`, `Boolean` or ULID column reaches the driver unprocessed | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` | `test_rows_land_identically_under_either_mode[driver]` (1 failure, proven 2026-08-26) | spec 139 |
| B2 | a bulk insert fills an omitted scalar default with `NULL` instead of the declared value | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` | `test_rows_land_identically_under_either_mode[driver]` (1 failure, proven 2026-08-26) | spec 139 |
| B3 | the profile's bulk mode is ignored (`mode = "driver"` for every dialect) — Core-pinned engines take the driver path | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` | `test_core_mode_never_touches_the_driver`, the copy-mode pins, `test_profiles_pin_the_measured_mode` (5 failures, proven 2026-08-26) | spec 139 |
| B4 | the copy prologue is dropped — a raw COPY issued as a transaction's first statement runs in asyncpg's autocommit | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` (+ the Postgres leg's `test_a_first_statement_bulk_insert_rolls_back`) | `test_copy_mode_streams_records_on_the_raw_connection` (1 failure, proven 2026-08-26) | spec 139 |
| B5 | the SQLSTATE wrap is dropped — a raw driver error reaches callers unclassified, so the savepoint re-drive never keys on `IntegrityError` | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` | `test_copy_mode_restores_the_classes_callers_catch` × 3 (3 failures, proven 2026-08-26) | spec 139 |
| B6 | driver-mode paging is voided (`page = len(rows)`) — one call carries the whole batch | `src/vfs/storage/backends/database/dialects.py` | `tests/storage/database/test_dialects.py` | `test_driver_mode_pages_by_the_parameter_budget` (the declared-value referee), the squeezed-budget spy, the copy paging pin (3 failures, proven 2026-08-26) | spec 139 |
| B7 | the write pipeline's chunk re-drive is lost (`except IntegrityError: raise` at the catch-retry chunk) — a conflicted chunk fails the batch instead of resolving row-wise | `src/vfs/storage/backends/database/writes.py` | `tests/storage/database/test_writes.py` | `test_catch_retry_arm_serves_the_write_family`, `test_conflicted_chunk_resolves_alone_not_the_layer`, `test_conflicts_in_separate_chunks_all_classify` (3 failures, proven 2026-08-26) | spec 139 |
